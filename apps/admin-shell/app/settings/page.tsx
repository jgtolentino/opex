// app/settings/page.tsx
export default function SettingsPage() {
  return (
    <div>
      <h1>Settings</h1>
      <p>
        User preferences (theme, AI toggles) and links to admin config will go
        here.
      </p>
    </div>
  );
}
